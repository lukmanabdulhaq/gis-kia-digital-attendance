{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && npx vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.11",
    "bcryptjs": "^3.0.2",
    "encore.dev": "^1.54.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^8.0.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}